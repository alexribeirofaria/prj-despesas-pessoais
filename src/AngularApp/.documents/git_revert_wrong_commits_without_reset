# Crie uma nova branch apartir da branch que deseja reveter
git checkout -b <new-branch-name>

# Listar commits realizados e encontrar hash que deseja reverter
git log

# Desfazer alterações do commit  original na branch atual
git revert <hash--commit>




keytool -keystore "$env:USERPROFILE\.android\production-keystore" -list -v

keytool -keystore  "$env:USERPROFILE\.android\debug.keystore"  -list -v

keytool -list -v -keystore "%USERPROFILE%\.android\prdocution.keystore" -alias productionkey -storepass masterMind81 -keypass masterMind81

keytool -list -v -alias production-keystore "$env:USERPROFILE\.android\production-keystore"

keytool -list -v -alias androiddebugkey -keystore "$env:USERPROFILE\.android\debug.keystore"
keytool -list -v -keystore "%USERPROFILE%\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android
