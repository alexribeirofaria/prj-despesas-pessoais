import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alexfariakof.ios',
  appName: 'app-despesas-pessoais-ios',
  webDir: '../www'
};

export default config;
